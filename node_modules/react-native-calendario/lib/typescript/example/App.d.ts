import React from 'react';
import { DayType, RangeType } from 'react-native-calendario';
declare type Props = {};
declare type State = {
    isVisibleModalCustom: boolean;
    minDate: Date;
    maxDate: Date;
    startDate: Date;
    endDate?: Date;
};
export default class App extends React.PureComponent<Props, State> {
    state: {
        startDate: Date;
        endDate: Date;
        minDate: Date;
        maxDate: Date;
        isVisibleModalCustom: boolean;
    };
    handleOpenCustomModal: () => void;
    handleCloseCustomModal: () => void;
    onChange: ({ startDate, endDate }: RangeType) => void;
    renderDayContent: (item: DayType) => JSX.Element;
    render(): JSX.Element;
}
export {};
