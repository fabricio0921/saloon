import React, { ComponentType } from 'react';
import { DayType, ThemeType } from '../../types';
interface Props {
    onPress: (date: Date) => void;
    item: DayType;
    theme: ThemeType;
    renderDayContent?: (day: DayType) => ComponentType;
}
declare const Day: React.NamedExoticComponent<Props>;
export default Day;
